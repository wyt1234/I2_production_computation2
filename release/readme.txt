1、最好用【以管理员身份打开】打开exe文件，不然可能会出现无法保存输入的文件路径的情况
2、步骤234等都依赖于步骤1的结果，所以步骤1必须执行（当天只需执行1次即可，不需要重复执行），其他步骤可自选执行
3、步骤1合并文件较大，使用了主线程执行，所以在最后保存时窗口可能出现短暂的【未响应】的情况，稍等一会儿即可
4、其他步骤234等使用子线程执行，理论上可以并发，但不建议一起，最好等一个执行完了再执行另一个
5、注意报错信息，如读取失败的excel是有问题的，格式啥的调整下，如果肉眼看不出区别就把原有内容复制保存到新的excel里